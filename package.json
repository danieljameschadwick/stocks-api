{
  "name": "stocks-api",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "babel src --out-dir dist && node dist/app.js",
    "start": "npm run build && node dist/app.js",
    "nodemon": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^7.0.0-bridge.0",
    "express": "^4.17.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.29"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11"
  }
}
